<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.gyw.community.system.mapper.SysUserMapper">

    <sql id="Example_Where_Clause">
        <where>
            <foreach collection="oredCriteria" item="criteria" separator="or">
                <trim prefix="(" suffix=")" prefixOverrides="and">
                    <foreach collection="criteria.criteria" item="criterion">
                        <choose>
                            <when test="criterion.noValue">
                                and ${criterion.condition}
                            </when>
                            <when test="criterion.singleValue">
                                and ${criterion.condition} #{criterion.value}
                            </when>
                            <when test="criterion.betweenValue">
                                and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                            </when>
                            <when test="criterion.listValue">
                                and ${criterion.condition}
                                <foreach collection="criterion.value" item="listItem" open="(" close=")" separator=",">
                                    #{listItem}
                                </foreach>
                            </when>
                        </choose>
                    </foreach>
                </trim>
            </foreach>
        </where>
    </sql>
    
    <resultMap id="userVO" type="cn.gyw.community.system.dto.UserDto">
        <result column="uid" property="uid" jdbcType="INTEGER"/>
        <result column="user_name" property="userName" jdbcType="VARCHAR"/>
        <result column="user_name_cn" property="userNameCn" jdbcType="VARCHAR"/>
        <result column="avatar" property="avatar" jdbcType="VARCHAR"/>
        <result column="age" property="age" jdbcType="INTEGER"/>
        <result column="hobby" property="hobby" jdbcType="VARCHAR"/>
        <!--
           assocication可以指定联合的JavaBean对象
           property="role"指定哪个属性是联合的对象
           javaType:指定这个属性对象的类型
         -->
        <association property="role" javaType="cn.gyw.community.system.dto.RoleDto">
            <result column="role_id" property="roleId" jdbcType="INTEGER"/>
            <result column="role_name" property="roleName" jdbcType="VARCHAR"/>
            <result column="role_status" property="roleStatus" jdbcType="INTEGER"/>
        </association>
    </resultMap>

    <select id="findWithRole" resultMap="userVO" parameterType="cn.gyw.components.web.model.QueryExample">
     select t1.uid, user_name,user_name_cn,avatar,password,age,hobby,role_id,role_name,role_status from (
       select uid, user_name,user_name_cn,password,age,hobby,avatar, role_id as rid from sys_user
       <if test="_parameter != null">
           <include refid="Example_Where_Clause"/>
       </if>
       ) t1
       inner join sys_role t2
       on t1.rid = t2.role_id
       ;
   </select>
</mapper>
